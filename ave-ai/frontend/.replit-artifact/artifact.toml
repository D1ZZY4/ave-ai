kind = "web"
previewPath = "/"
title = "Ave AI"
version = "1.0.0"
id = "ave-ai/frontend"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 8081

[services.development]
run = "pnpm --filter @workspace/ave-ai run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ave-ai", "run", "build" ]
publicDir = "ave-ai/frontend/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "8081"
BASE_PATH = "/"
